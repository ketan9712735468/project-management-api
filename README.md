# Project Management API

This is a Project Management API built with Django, Django REST Framework, and JWT for token-based authentication. The API supports user registration, login, and CRUD operations on projects, tasks, and comments.

## Features
- **User Management:** Register, login, update, and delete users.
- **Projects:** Create, update, and manage projects.
- **Tasks:** Add tasks to projects, update, delete, and retrieve tasks.
- **Comments:** Add comments to tasks and manage them.

## Requirements

Ensure you have the following installed:
- **Python** 3.8+
- **PostgreSQL** (for database)
- **pip** (Python package manager)
- **Git**

## Project Setup

### 1. Clone the Repository
```bash
git clone https://github.com/your-username/project-management-api.git
cd project-management-api
```

### 2. Set up a Virtual Environment
- **On Windows**
```bash
python -m venv venv
venv\Scripts\activate
```
- **On macOS/Linux**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Install Dependencies
```bash
pip install -r requirements.txt
```

### 4. Database Setup
- **Open the PostgreSQL shell**
```bash
sudo -u postgres psql
```
- **create a new database**
```bash
create database tech_foring;
```
Ensure your postgres user name and password is "postgres", if not then chnage user password using below command into psql shell:
```bash
ALTER USER postgres WITH PASSWORD 'postgres';
```
or you can use your current user name and password into techforing/settings.py DATABASES configuration


### 5. Migrate the Database
```bash
python3 manage.py migrate
```

### 6. Run the Development Server
```bash
python manage.py runserver
```

### 7. API Documentation
- **Swagger**
To access the API documentation generated by Swagger, visit:
http://127.0.0.1:8000/swagger/

- **ReDoc**
To access the API documentation generated by ReDoc, visit:
http://127.0.0.1:8000/redoc/